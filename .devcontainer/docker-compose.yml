version: '3'
services:
  ldapserver:
      image: ghcr.io/getgatekeeper/scim-client-devcontainer:sha-a327ba4
      volumes:
        - .:/workspace:cached
        - /var/run/docker.sock:/var/run/docker-host.sock
      command: /bin/sh -c "while sleep 1000; do :; done"
